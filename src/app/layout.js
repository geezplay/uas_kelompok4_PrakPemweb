import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Race Setup Garage - Sistem Manajemen Setup Motor Balap",
  description: "Platform digital pencatatan setup motor balap secara digital. Tinggalkan kertas manual dan simpan spesifikasi rasio gir, jetting karburator, timing pengapian, dan temperatur sirkuit dengan rapi.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
